substring(string_name,start index,length);
replace(string_name,character1,character2);
length(string_name);
min(),max(),count(),avg(),sum(),aggregate();
DATEDIFF(1,2)=value;
limit n-1,length; // for example limit(1,1) means 2nd row and length 1 //n dpeicts the start point.